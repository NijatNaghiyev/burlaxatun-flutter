import 'package:flutter/material.dart';

class DailyAdvisePage extends StatelessWidget {
  const DailyAdvisePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DAILY ADVISE VIEW'),
      ),
    );
  }
}
