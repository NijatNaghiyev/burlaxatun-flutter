import 'package:flutter/material.dart';

class AdviseImage extends StatelessWidget {
  const AdviseImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
