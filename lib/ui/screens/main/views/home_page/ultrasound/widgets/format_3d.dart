import 'package:flutter/material.dart';

class Format3d extends StatelessWidget {
  const Format3d({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('3D'),
    );
  }
}
