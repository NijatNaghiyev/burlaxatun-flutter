import 'package:flutter/material.dart';

class AdvisesPage extends StatelessWidget {
  const AdvisesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ADVISES'),
      ),
      body: Container(),
    );
  }
}
